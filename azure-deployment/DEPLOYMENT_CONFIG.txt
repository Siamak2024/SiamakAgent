# EA Platform Configuration
# ⚠️ WARNING: DO NOT STORE SECRETS IN THIS FILE ⚠️
# 
# This file is for documentation only. All secrets must be stored in:
# - Local development: .env file (not committed to Git)
# - Azure deployment: Azure Portal → Configuration → Environment variables

# SECURITY UPDATE (May 15, 2026):
# The old configuration with hardcoded secrets has been removed for security.
# Please follow the instructions in SECURITY_SETUP.md to configure properly.

# APP_BASE_URL=https://your-app-name.azurestaticapps.net

# Copy these to Azure Portal:
# Azure Portal → Static Web Apps → Your App → Configuration → Environment variables

# Required variables:
# 1. ADMIN_SECRET_KEY = <generate using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))">
# 2. APP_BASE_URL = https://your-app-name.azurestaticapps.net
# 3. OPENAI_API_KEY = sk-your-key-here
# 4. ALLOWED_ORIGINS = https://your-app-name.azurestaticapps.net
# 5. ea_invites_data = {}
# 6. ea_sessions_data = {}

# See SECURITY_SETUP.md for complete setup instructions
